import { Button } from '@mui/material'
import { motion } from 'framer-motion'
import botanical from '../../assets/florals/editorial-botanical.webp'
import { wedding } from '../../config/wedding.config'
import { Countdown } from '../common/Countdown'
export function HeroSection() { const items=[<div className="hero-monogram" key="m">{wedding.couple.initials}</div>,<p className="hero-kicker" key="k">¡Nos casamos!</p>,<h1 id="hero-title" key="h"><span>{wedding.couple.first}</span><i>&</i><span>{wedding.couple.second}</span></h1>,<p className="hero-date" key="d">{wedding.displayDate}</p>,<div className="countdown-veil" key="c"><Countdown/></div>,<Button key="b" variant="contained" className="luminous-button" onClick={()=>document.getElementById('rsvp')?.scrollIntoView()}>Acompañanos</Button>];return <section className="hero-cinematic" aria-labelledby="hero-title" style={{backgroundImage:`url(${wedding.images.hero})`}}><motion.div className="hero-backdrop" initial={{scale:1.08}} animate={{scale:1}} transition={{duration:3,ease:'easeOut'}}/><motion.div className="silk-veil" initial={{x:'-100%',rotate:-16}} animate={{x:'12%',rotate:-8}} transition={{duration:1.5,ease:[.22,1,.36,1]}}/><img className="hero-floral" src={botanical} alt=""/ ><div className="hero-content">{items.map((item,i)=><motion.div key={i} initial={{opacity:0,y:22,filter:'blur(8px)'}} animate={{opacity:1,y:0,filter:'blur(0px)'}} transition={{delay:.4+i*.17,duration:.75}}>{item}</motion.div>)}</div><p className="scroll-cue">Deslizá para descubrir <i/></p></section> }
